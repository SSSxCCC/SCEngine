cmake_minimum_required (VERSION 3.18.1)
project ("vulkanapp")
add_library (vulkanapp SHARED main.cpp AndroidOut.cpp "../../../../../Common/sc_platform/PlatformImpl.cpp")
target_include_directories (vulkanapp PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} "../../../../../Common")
set_property (TARGET vulkanapp PROPERTY CXX_STANDARD 20)
find_library (log-lib log)
find_package (game-activity REQUIRED CONFIG)
add_subdirectory ("D:/testProject/test1" "SCEngine")
target_link_libraries (vulkanapp android game-activity::game-activity jnigraphics ${log-lib} vulkan SCEngine)
